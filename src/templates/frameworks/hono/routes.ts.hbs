import { Hono } from "hono";

const routes = new Hono();

routes.get("/", (c) => {
  return c.json({
    message: "Welcome to {{projectName}}",
    framework: "{{framework}}",
    version: "1.0.0",
  });
});

// Add your routes here

export { routes };